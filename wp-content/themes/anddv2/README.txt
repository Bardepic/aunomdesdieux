Une fois Yarn Installé, pour initialer, penser à créer un fichier env.js (copie de celui en .dist dans le fichier config)
et lui mettre l'adresse local de son projet.

Par défaut, on trouve le site à localhost:3010
(configuré par Anthony)

Penser à lancer Yarn Start avant de bosser sur le site.